spring:
  application:
    name: configservice
  profiles:
    active: netposa

server:
  port: 8080

logging:
  file: ./logs/centerconf/netposa-configservice.log

endpoints:
  health:
    sensitive: false

management:
  security:
    enabled: false
  health:
    status:
      order: DOWN, OUT_OF_SERVICE, UP, UNKNOWN
